| #   | Cycle                                                                                   | Strategy                                                                                                                                                                                                                                                                                                                                                 | Status | Reason                                                                                                                                     |
| --- | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | `config/ConfigService.js → outputs/index.js → outputs/LocalOutput.js → config/index.js` | Dependency Injection: add resolver setters to `ConfigService` (`setOutputStrategyResolver`, `setOutputSecretKeysResolver`) and `migrationService` (`setOutputSecretKeysResolver`); wire resolvers in `server.js` after output registration so config-side modules never import the `outputs` barrel. Each output strategy keeps its own inline metadata. | ✅     | Cycle broken at both `ConfigService → outputs` and `migrationService → outputs` edges. Madge clean. 799 backend + 572 frontend tests pass. |
