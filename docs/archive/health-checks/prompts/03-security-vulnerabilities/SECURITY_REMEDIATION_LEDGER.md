# Security Remediation Ledger

**Branch:** `fix/security-vulnerabilities`
**Created:** 2026-04-13
**Last Updated:** 2026-04-13

## Semgrep Findings

| Tool    | File Path                              | Line(s) | Rule ID                                                                                               | Severity | Status | Reason                                                                                                                                                                                                                                                                                                                                                           |
| ------- | -------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------- | -------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Semgrep | src/outputs/BaseOutput.js              | 78      | javascript.lang.security.audit.path-traversal.path-join-resolve-traversal.path-join-resolve-traversal | Blocking | ✅     | Replaced direct `path.resolve(...)` sink with normalized sidecar path construction and retained `isWithinRoot(SRC_PUBLIC_ROOT, metaPath)` boundary enforcement. Focused regression tests passed and focused Semgrep rerun returned 0 findings. Post-fix GitNexus impact/context retry was attempted twice but blocked by MCP `Not connected`.                    |
| Semgrep | src/services/core/automationService.js | 126     | javascript.lang.security.audit.path-traversal.path-join-resolve-traversal.path-join-resolve-traversal | Blocking | ✅     | Removed direct `path.join(...)` on untrusted file setting, canonicalized through `normalizeSource(...)`, preserved fail-closed behavior for invalid traversal input, and verified with focused regression tests plus a clean focused Semgrep rerun. Post-fix GitNexus impact/context retry was attempted twice but blocked by MCP `Not connected`.               |
| Semgrep | src/services/core/automationService.js | 212     | javascript.lang.security.audit.unsafe-formatstring.unsafe-formatstring                                | Blocking | ✅     | Replaced interpolated format string with a constant `console.error` format string and separate arguments, then updated regression coverage and confirmed the focused Semgrep rerun is clean. Post-fix GitNexus impact/context retry was attempted twice but blocked by MCP `Not connected`.                                                                      |
| Semgrep | src/utils/normalizeSource.js           | 115     | javascript.lang.security.audit.path-traversal.path-join-resolve-traversal.path-join-resolve-traversal | Blocking | ✅     | Replaced direct `path.resolve(...)` sink with normalized absolute path construction while keeping `_validatePathTraversal(...)` and `isWithinRoot(...)` containment checks intact. Focused regression tests passed and focused Semgrep rerun returned 0 findings. Post-fix GitNexus impact/context retry was attempted twice but blocked by MCP `Not connected`. |
| Semgrep | src/utils/pathUtils.js                 | 10-11   | javascript.lang.security.audit.path-traversal.path-join-resolve-traversal.path-join-resolve-traversal | Blocking | ✅     | Hardened `isWithinRoot(...)` to reject non-absolute inputs and compare normalized absolute paths, added dedicated unit coverage, and verified with focused regression tests plus a clean focused Semgrep rerun. Post-fix GitNexus impact/context retry was attempted twice but blocked by MCP `Not connected`.                                                   |

## Bandit Findings

No active Bandit findings in the current initial audit (`src/microservices/tts/server.py`, `test_sanitisation.py`, `test_security.py`). Prior findings are no longer reproducible in repo code.
