# Quality Commands

> Referenced by opencode commands via `@.opencode/quality.md`.
> Fill in the commands for your project. Use `N/A` to skip a check.
> Multiple commands can be comma-separated (e.g. `ruff check, ruff format --check`).

## Frontend

- **Lint:** npm run lint
- **Test:** npm run test:client
- **Build:** npm run build (in client folder)

## Backend

- **Lint:** N/A
- **Test:** npm run test:src
- **Build:** N/A
