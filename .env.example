# Admin credentials
ADMIN_USERNAME=admin
# Generate password hash using the following command:
# node -e "console.log(require('crypto').createHash('sha256').update('YOUR_PASSWORD' + 'YOUR_SALT').digest('hex'))"
ADMIN_PASSWORD_HASH=

# Optional: Salt for additional security (recommended)
SALT=

# Voice Monkey API token (if using voice announcements)
VOICEMONKEY_TOKEN= 